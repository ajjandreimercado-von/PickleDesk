
  # Build PickleDesk App

  This is a code bundle for Build PickleDesk App. The original project is available at https://www.figma.com/design/JdtvTzECV5h9r5Se9U7RL4/Build-PickleDesk-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  